{$DOMAIN} {
    # API and WebSocket — forward to the PM2-managed Node server
    reverse_proxy /api/* localhost:3001
    reverse_proxy /socket.io/* localhost:3001

    # Everything else — serve the built React app
    root * /app/client/dist
    encode gzip
    file_server
    try_files {path} /index.html
}
